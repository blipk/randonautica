// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.lambdoma.randonautica"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
